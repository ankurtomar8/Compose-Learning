<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComposeLearning">

        <activity
            android:name=".activity.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ComposeLearning">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.CheckBoxActivity" android:exported="false" />
        <activity android:name=".activity.NavigationViewActivity" android:exported="false" />
        <activity android:name=".activity.ColumnViewActivity" android:exported="false" />
        <activity android:name=".activity.RowViewActivity" android:exported="false" />
        <activity android:name=".activity.RecyclerViewActivity" android:exported="false" />
        <activity android:name=".activity.RadioButtonActivity" android:exported="false" />
        <activity android:name=".activity.PickImageActivity" android:exported="false" />
        <activity android:name=".activity.DialogActivity" android:exported="false" />
        <activity android:name=".navigation.navigation.AnotherActivity" android:exported="false" />

    </application>

</manifest>
